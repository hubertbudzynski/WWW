### Compliation and setting up the database
Run
```
$ npm install && cd /quiz && npm install
$ npm run build && npm run createdb
```

Start server
```
$ npm run start
```

Test
```
$ npm run test
```

